<div class="card">
  <span class="heading text-center">Set the list of tellers</span>
  <%= form_with class: "contents", method: "put" do |f| %>
    <%= f.text_area :tellers, spellcheck: false, class: "text-box text-area" %>
    <%= f.submit "Set", class: "btn" %>
  <% end %>
</div>
